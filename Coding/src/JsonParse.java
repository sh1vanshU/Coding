import java.io.IOException;

class JsonParse{
    public static void main(String[] args) throws IOException {
        // String inline="";
        try{

            
            // URL url=new URL("https://jsonmock.hackerrank.com/api/articles?author=olalonde&page=1");
            // HttpsURLConnection connection=(HttpsURLConnection)url.openConnection();
            // connection.setRequestMethod("GET");
            // connection.connect();
            // int responseCode=connection.getResponseCode();  
            // System.out.println("Response Code is :" + responseCode);
            // int limit=1;
            // if(responseCode!=200)
            // {
            //     throw new RuntimeException("HttpResponseCode  :  "+responseCode);
            // }else{
            //     InputStream inputStream=url.openStream();
            //     InputStreamReader iStreamReader=new InputStreamReader(inputStream,StandardCharsets.UTF_8);
            //     BufferedReader br=new BufferedReader(iStreamReader);
            //     br.lines().forEach( line -> System.out.println(line));

                
            // }
            
            
        }catch(Exception e){
            e.printStackTrace();
        }
    }
}